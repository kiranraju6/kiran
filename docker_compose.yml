version : '3.8'
services :

  jenkins:
    image : jenkins/jenkins:lts
    ports: 
      - "8080:8080"
      - "50000:50000"
    volumnes:
      -jenkins_home:var/jenkins_home
    restart : unless-stopped
  flaskapp:
      build:
      ports :
        - "5000-5000"
      restart : unless-stopped


